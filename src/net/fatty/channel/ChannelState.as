package net.fatty.channel {
    public final class ChannelState {
        public static const OPEN : ChannelState = new ChannelState();
        public static const CONNECTED : ChannelState = new ChannelState();
    }
}
